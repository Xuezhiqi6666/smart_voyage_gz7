# SmartVoyage 智能旅行助手 — 项目文档

> 文档说明：本文档基于对工作文件夹 `smart_voyage_gz7` 全量源码阅读整理生成，覆盖项目架构、目录结构、核心模块、数据层、前端、测试与部署信息。

---

## 1. 项目概述

**SmartVoyage** 是一个基于 **A2A（Agent-to-Agent）协议 + MCP（Model Context Protocol）工具服务** 的多代理旅行助手系统，支持通过自然语言完成：

- **天气查询**（和风天气 API / MySQL）
- **票务查询与预订**（火车票 / 机票 / 演唱会门票）
- **行程服务**（租车 / 旅游团 / 旅行保险 的查询与预订）
- **景点推荐**（LLM 直连生成，结合真实天气）

系统采用 **三层架构**：`API 层 → A2A 代理层 → MCP 工具层`，整体由 `api_server.py` 一键启动（自动拉起 6 个子服务）。

---

## 2. 系统架构

```
用户 (浏览器/CLI)
    │
    ▼
┌─────────────────────────────────────────────┐
│  api_server.py (FastAPI :8080)              │
│  chat_service.py (意图识别 → 路由 → 执行)    │
└─────────────────┬───────────────────────────┘
                  │ A2A 协议
    ┌─────────────┼─────────────┐
    ▼             ▼             ▼
┌────────┐  ┌────────┐  ┌────────┐
│Weather │  │Ticket  │  │Trip    │   A2A 代理层
│:5005   │  │:5006   │  │:5007   │   (LangChain 1.x create_agent)
└───┬────┘  └───┬────┘  └───┬────┘
    │           │           │
    ▼           ▼           ▼
┌────────┐  ┌────────┐  ┌────────┐
│MCP     │  │MCP     │  │MCP     │   MCP 工具层
│:8002   │  │:8001   │  │:8003   │   (数据库/API查询)
└────────┘  └────────┘  └────────┘
    │           │           │
    ▼           ▼           ▼
 和风天气API   MySQL       MySQL+Milvus
```

### 2.1 核心对话流程

```
用户输入 → 意图识别(LLM) → 启发式路由 → 并行执行 / ReAct 循环 → 返回回复
```

1. **意图识别**（`intent_agent`）：LLM 分析输入，返回 JSON `{"intents": [...], "user_queries": {...}, "follow_up_message": "..."}`
2. **启发式路由**（`_should_skip_planning`）：单意图或独立多意图跳过规划，直接并行执行，省去 1 次 LLM 调用
3. **ReAct 循环**（`react_loop`）：复杂任务按 `depends_on` 分组，组内 `asyncio.gather` 并行执行，最终由 LLM 汇总

### 2.2 服务端口一览

| 层 | 服务 | 端口 | 脚本 |
|---|---|---|---|
| 主服务 | FastAPI API | 8080 | `api_server.py` |
| A2A 代理 | WeatherAssistant | 5005 | `a2a_server/weather_server.py` |
| A2A 代理 | TicketAssistant | 5006 | `a2a_server/ticket_server.py` |
| A2A 代理 | TripAssistant | 5007 | `a2a_server/trip_server.py` |
| MCP 工具 | TicketTools | 8001 | `mcp_server/mcp_ticket_server.py` |
| MCP 工具 | WeatherTools | 8002 | `mcp_server/mcp_weather_server.py` |
| MCP 工具 | TripTools | 8003 | `mcp_server/mcp_trip_server.py` |
| 基础设施 | MySQL 8.0 | 3307→3306 | `milvus_redis_mysql/docker-compose.yml` |
| 基础设施 | Milvus | 19530 | 同上 |
| 基础设施 | Redis | 6379 | 同上 |
| 基础设施 | MinIO / etcd | 9000 / 2379 | 同上 |

---

## 3. 目录结构

```
smart_voyage_gz7/
├── api_server.py              # FastAPI 主服务，自动启动所有子服务（~260行）
├── chat_service.py            # 核心对话服务：意图识别+路由+执行（~1036行）
├── config.py                  # 全局配置：LLM/DB/意图映射/天气/向量库（~171行）
├── main_prompts.py            # Prompt 模板管理（~314行）
├── memory.py                  # 三层记忆管理：短期对话/用户偏好/实体历史（~345行）
├── create_logger.py           # 日志模块（控制台 UTF-8 + 文件 DEBUG）（~61行）
├── requirements.txt           # 依赖清单（131 个包，含 LangChain 1.x / python-a2a / FastMCP）
├── .env                       # 环境变量（DASHSCOPE_API_KEY）
├── README.md                  # 项目说明 + 13 道常见面试题（~414行）
│
├── a2a_server/                # A2A 代理层（LangChain Agent + MCP Tools）
│   ├── weather_server.py      # 天气代理 :5005（~318行）
│   ├── ticket_server.py       # 票务代理 :5006（~337行）
│   └── trip_server.py         # 行程代理 :5007（~403行）
│
├── mcp_server/                # MCP 工具层（数据查询接口，FastMCP）
│   ├── mcp_ticket_server.py   # 票务工具 :8001（~261行）
│   ├── mcp_weather_server.py  # 天气工具 :8002（~333行）
│   └── mcp_trip_server.py     # 行程工具 :8003（~655行）
│
├── utils/
│   ├── mcp_tools.py           # MCP → LangChain StructuredTool 桥接（修复可选参数 bug）（~177行）
│   ├── format.py              # JSON 编码器（date/Decimal/timedelta）（~32行）
│   └── spider_weather.py      # 天气数据爬虫 + 定时更新（~406行）
│
├── sql/
│   ├── create_all_tables.sql  # 全量建表脚本（8 张表）
│   ├── insert_data.sql        # 8月1日测试数据（火车票/机票/演唱会/租车/保险）
│   ├── execute_sql.py         # SQL 文件执行脚本（按分号拆分逐条执行）
│   └── init_tour_group_rag.py # 旅游团 RAG 初始化（Embedding + Milvus 入库 + 测试检索）（~496行）
│
├── milvus_redis_mysql/
│   ├── docker-compose.yml     # MySQL + Milvus + etcd + MinIO + Redis 编排
│   └── volumes/               # 数据持久化目录（已 gitignore）
│
├── static/
│   └── index.html             # 前端单页（聊天 + Agent 卡片 + 记忆面板）（~485行）
│
├── test_api_server.py         # REST API 端点测试（10 项）
├── test_chat_service.py       # ChatService 端到端测试（10 项）
├── test_main_prompts.py       # Prompt 模板结构与 LLM 调用测试（9 项）
├── logs/app.log               # 运行日志
└── 项目文档.md                 # 本文档
```

---

## 4. 核心模块详解

### 4.1 主服务层

#### `api_server.py` — FastAPI 主服务
- **接口**：`/`（前端页）、`POST /api/chat`、`POST /api/chat/stream`（SSE）、`GET /api/agents`、`GET /api/memory`、`POST /api/memory/clear`、`POST /api/memory/profile`
- **子服务自动管理**：启动时按「MCP 工具层 → A2A 代理层」顺序拉起 6 个子进程，`stderr` 用后台线程排空（防止 PIPE 缓冲区满阻塞），启动后做端口健康检查（最多重试 10 次）；退出时通过 `atexit` + 信号处理器统一清理
- **SSE 心跳**：Producer/Queue 模式，意图识别与 A2A 调用在后台 Task 中执行，主循环每 10 秒无数据时发送 `: heartbeat` 注释行保活

#### `chat_service.py` — 核心对话服务（约 1036 行）
- **`intent_agent()`**：LLM 意图识别，支持 `weather / flight / train / concert / order / car_rental / tour_group / insurance / trip_order / attraction / complex / out_of_scope`，输出前用正则清理 ```json``` 代码块
- **`_should_skip_planning()`**：启发式路由 —— 单意图或全为独立查询类意图时跳过 Planning（省 1 次 LLM 调用）
- **`planning_agent()`**：复杂任务拆分为带 `depends_on` 依赖的步骤列表
- **`_call_agent_intent()`**：统一执行入口 —— `attraction` 直连 LLM 生成（先查目的地天气）；其他意图通过 A2A 协议调用子代理（`run_in_executor` 包装阻塞的 `send_task_async`，超时 120s）；构建 agent 输入时只传最近 2 轮「干净」对话（过滤错误消息）
- **`react_loop()` / `_react_loop_stream()`**：按 `depends_on` 分组、组内 `asyncio.gather` 并行执行，跳过 Thought 推理（省略每步 1 次 LLM 调用），多步骤后用 `react_summary_prompt` 汇总
- **`chat_stream()` / `_call_agent_intent_stream()`**：流式版本，LLM 部分用 `astream` 逐块输出
- **`get_agent_cards()` / `get_memory_state()` / `clear_memory()`**：供前端侧边栏展示
- **性能优化总结**（代码注释明确列出）：启发式路由、省略 Thought、`asyncio.gather` 并行、`_cached_tools` 工具缓存、子 Agent 自行格式化输出跳过总结

#### `main_prompts.py` — Prompt 模板管理
| 模板 | 用途 |
|---|---|
| `intent_prompt()` | 意图识别，输出 JSON（intents / user_queries / follow_up_message） |
| `summarize_weather_prompt()` | 天气结果 → 自然语言（100-150 字） |
| `summarize_ticket_prompt()` | 票务结果 → 顾问式推荐 |
| `attraction_prompt()` | 景点推荐（3-5 个，天气为空时禁止虚构） |
| `planning_prompt()` | 任务复杂度评估 + 执行计划（steps + depends_on） |
| `react_prompt()` | ReAct 逐步推理（当前版本主流程未使用，保留学习参考） |
| `react_summary_prompt()` | 多步骤结果汇总为连贯回复（150-300 字） |

#### `memory.py` — 三层记忆（MySQL 持久化）
| 记忆类型 | 内存结构 | 数据库表 | 持久化策略 |
|---|---|---|---|
| 短期对话 | `short_term_messages[]`（上限 10 条） | `short_term_messages` | DELETE + 批量 INSERT |
| 用户偏好 | `user_profile{}` | `user_profiles` | INSERT ... ON DUPLICATE KEY UPDATE |
| 实体历史 | `entity_history[]`（上限 50 条） | `query_history` | 单条 INSERT |

- 服务启动时通过 `load_*_from_db()` 三方法自动恢复记忆
- 提供 `to_dict() / from_dict()` 序列化接口
- `_ensure_db()` 自动重连，失败时清理死连接

#### `config.py` — 全局配置
- LLM：`qwen3.7-max`，DashScope 兼容模式 `https://dashscope.aliyuncs.com/compatible-mode/v1`
- 数据库：`smart_voyage / 123456 @ travel_rag`，连接池 `pool_size=5`（注：配置端口 3306，与 docker-compose 宿主机 3307 注释有出入，代码注释已提示保持一致）
- 意图 → 代理映射：`weather→WeatherAssistant`、`flight/train/concert/order→TicketAssistant`、`car_rental/tour_group/insurance/trip_order→TripAssistant`
- 天气：和风天气 API（`weather_source = "api"` 直接调 API；`"database"` 查 MySQL），监控城市北京/成都，每日 01:00 定时更新
- Milvus：`tour_groups` 集合，Qwen Embedding 1024 维
- `ensure_mysql_user()`：用 root 自动创建业务账号（docker-compose 已配置 MYSQL_USER 时可跳过）

#### `create_logger.py` — 日志模块
- 控制台处理器（INFO，强制 UTF-8 包装 stderr 解决 Windows GBK 中文乱码）+ 文件处理器（DEBUG，`logs/app.log`）
- `logger.propagate = False` + 重复 handler 判断，避免重复输出

### 4.2 A2A 代理层（三个子代理结构一致）

每个子代理 = `A2AServer 子类` + `AgentCard（名称/技能/描述）` + `LangChain Agent`：

| 代理 | 端口 | MCP 依赖 | Agent 实现 |
|---|---|---|---|
| WeatherAssistant | 5005 | `utils.mcp_tools.to_structured_langchain_tool(":8002")` | `create_tool_calling_agent` + `AgentExecutor`（langchain 0.x 写法） |
| TicketAssistant | 5006 | 同上（":8001"） | 同上，`max_iterations=5`，同步 `invoke()` |
| TripAssistant | 5007 | 同上（":8003"） | **LangChain 1.x**：`create_agent(llm, tools, system_prompt=...)` + `agent.ainvoke(config={"recursion_limit": 15})`，捕获 `ExceptionGroup` |

**追问机制**（三个代理一致）：
- LLM 信息不足时回复以 `[追问]` 开头 → 代理置 `TaskState.INPUT_REQUIRED`，追问内容放 `task.status.message`
- 兜底关键词判断：`请提供 / 请告诉我 / 请问您 / 哪一天 / 哪个城市` 等
- 防误判：输出包含真实数据特征（`¥`、`车次`、`航班`、`余票`、`元/天` 等）时不视为追问
- 成功 → `TaskState.COMPLETED`，结果放 `task.artifacts`；异常 → `TaskState.FAILED`

### 4.3 MCP 工具层（FastMCP + 参数化 SQL）

| 服务 | 工具 | 数据源 |
|---|---|---|
| TicketTools :8001 | `query_train` / `query_flight` / `query_concert`（可选参数：seat/cabin/ticket_type） | MySQL 参数化查询（`%s` 占位符防注入） |
| | `order_train` / `order_flight` / `order_concert` | **模拟返回**（TODO 未写库） |
| WeatherTools :8002 | `query_weather(city, start_date, end_date)` | 按 `conf.weather_source` 切换：API（geo 查 ID → 天气接口 → 过滤日期映射字段）或 MySQL |
| TripTools :8003 | `query_car_rental`（MySQL）、`query_tour_group`（**Milvus 向量语义检索 RAG**）、`query_insurance`（MySQL） | 混合数据源 |
| | `order_car_rental` / `order_tour_group` / `order_insurance` | **模拟返回** |

**MCP 层共性**：
- `MySQLConnectionPool` 连接池复用连接，`finally` 中归还
- `dictionary=True` 游标 + `DateEncoder` / `default_encoder` 处理 date/datetime/Decimal 序列化
- 返回值统一 JSON 结构：`{"status": "success"/"no_data"/"error", ...}`

### 4.4 工具模块

#### `utils/mcp_tools.py` — MCP 桥接（关键修复）
- **解决的问题**：`python_a2a` 的 `to_langchain_tool()` 生成无 `args_schema` 的普通 Tool，可选参数会报 `"Too many arguments to single-input tool"`
- **方案**：从 `/tools` 端点取 JSON Schema → `pydantic.create_model` 动态构建参数模型（必填无默认值 / 可选包 `Optional` 默认 None）→ 生成 `StructuredTool`，执行函数 POST 到 `/tools/{name}`

#### `utils/spider_weather.py` — 天气爬虫
- 从和风天气 30 天预报接口拉取（gzip 解压），`ON DUPLICATE KEY UPDATE` UPSERT 入库（唯一键 `unique_city_date(city, fx_date)`）
- 判断逻辑：无数据 / 距今超 24 小时 / `force_update=True` 才更新；`schedule` 每日定时（仅 `weather_source != "api"` 时启动）
- 内置建表语句与完整数据流注释

---

## 5. 数据层

### 5.1 MySQL（travel_rag，8 张表）

| 表 | 说明 | 关键字段 / 唯一键 |
|---|---|---|
| `train_tickets` | 火车票 | departure/arrival_city、departure_time、train_number、seat_type、price、remaining_seats；`unique_train` |
| `flight_tickets` | 机票 | 同构，flight_number、cabin_type；`unique_flight` |
| `concert_tickets` | 演唱会票 | artist、city、venue、ticket_type；`unique_concert` |
| `weather_data` | 天气（爬虫写入） | 含日出日落/温度/风向/湿度等 29 字段；`unique_city_date` |
| `car_rentals` | 租车 | company、car_type、price_per_day、deposit；`unique_car` |
| `insurances` | 保险 | 综合/意外/医疗/境外四型；`unique_insurance` |
| `user_profiles` | 用户偏好（记忆） | `profile_key` 主键 |
| `query_history` | 查询历史（记忆） | intent_type、query_content(JSON)、query_time |
| `short_term_messages` | 短期对话（记忆） | role、content、message_order |

### 5.2 Milvus（tour_groups 集合，旅游团 RAG）

- 向量：`text-embedding-v3` 生成 1024 维，索引 `IVF_FLAT` + `COSINE`
- 字段：tour_id / tour_name / city / days / price / seats / agency / rating / departure_dates / highlights / embedding
- 数据：`sql/init_tour_group_rag.py` 内置 24 个旅游团（云南/四川/北京/上海/海南/西藏/桂林/西安），`mcp_trip_server.py` 启动时若集合不存在则自动建集合
- 检索链路：用户查询 → Embedding → Milvus search（可选 city 过滤，nprobe=10）→ 相似度排序返回 top-5

### 5.3 Docker Compose（milvus_redis_mysql/docker-compose.yml）

| 服务 | 镜像 | 端口 | 说明 |
|---|---|---|---|
| mysql | mysql:8.0 | 3307→3306 | root 密码 123456，自动创建 `smart_voyage` 用户 + `travel_rag` 库，utf8mb4，TZ=Asia/Shanghai，healthcheck |
| standalone | milvusdb/milvus:v2.4.4 | 19530 / 9091 | 依赖 etcd + minio |
| etcd | quay.io/coreos/etcd:v3.5.5 | 2379 | Milvus 元数据 |
| minio | minio/minio | 9000 | 对象存储（minioadmin/minioadmin） |
| redis | redis:latest | 6379 | 密码 1234 |

---

## 6. 前端（static/index.html）

- 深色主题单页应用（暗蓝 `#1a1a2e` + 主色 `#e94560`），自适应布局：左侧聊天区 + 右侧侧边栏
- **功能**：
  - SSE 流式对话：`fetch` 读取 `POST /api/chat/stream`，逐 chunk 渲染 + 光标闪烁动画 + 打字指示器
  - Agent Cards 面板：`GET /api/agents` 展示代理名称/描述/技能/在线状态
  - Memory 面板：`GET /api/memory` 展示短期对话/用户偏好（可编辑增删保存）/当前任务/查询历史
  - 清空记忆按钮：`POST /api/memory/clear`
- 底部标注：`Powered by 黑马程序员 | 基于Agent2Agent的旅行助手系统 v7.5`

---

## 7. API 接口

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/` | 返回前端页面 |
| POST | `/api/chat` | 发送消息获取完整回复（JSON `{message}`） |
| POST | `/api/chat/stream` | SSE 流式回复（`data: {"chunk": ...}`，结束 `data: [DONE]`，心跳 `: heartbeat`） |
| GET | `/api/agents` | 代理卡片列表 |
| GET | `/api/memory` | 记忆状态（短期对话/偏好/任务/实体历史） |
| POST | `/api/memory/clear` | 清空记忆（含数据库） |
| POST | `/api/memory/profile` | 更新用户偏好（`{"profile": {...}}`，UPSERT 合并） |

---

## 8. 环境依赖与启动

### 依赖（requirements.txt 要点）
- Python 3.11+；核心：`langchain==1.3.14`、`langgraph==1.2.9`、`python-a2a==0.5.4`、`mcp==1.18.0`、`fastapi==0.116.1`、`pymilvus`（随 langchain 生态）、`mysql-connector-python==9.4.0`、`dashscope==1.24.0`、`torch==2.7.1`
- Docker（MySQL + Milvus）；DashScope API Key（`.env` → `DASHSCOPE_API_KEY`）

### 安装与启动
```bash
# 1. 启动基础设施
cd milvus_redis_mysql && docker-compose up -d

# 2. 初始化数据库
python sql/execute_sql.py sql/create_all_tables.sql
python sql/execute_sql.py sql/insert_data.sql
# （可选）旅游团向量库初始化
python sql/init_tour_group_rag.py

# 3. 一键启动（自动拉起 6 个子服务 + 主 API）
python api_server.py
# 访问 http://127.0.0.1:8080
```

### 手动分步启动（调试）
```bash
# MCP 工具层 :8001 :8002 :8003
python mcp_server/mcp_ticket_server.py
python mcp_server/mcp_weather_server.py
python mcp_server/mcp_trip_server.py
# A2A 代理层 :5005 :5006 :5007
python a2a_server/weather_server.py
python a2a_server/ticket_server.py
python a2a_server/trip_server.py
# 主服务 :8080
python api_server.py
```

---

## 9. 测试文件

| 文件 | 覆盖范围 | 测试项 |
|---|---|---|
| `test_api_server.py` | REST API 端点（需服务运行于 8080） | 首页、agents、memory 增删查、chat 天气/火车票/out_of_scope、SSE 流式（10 项） |
| `test_chat_service.py` | ChatService 端到端（直接实例化） | 单/多意图、out_of_scope、景点推荐、旅游团/保险、多轮记忆、流式输出（10 项） |
| `test_main_prompts.py` | Prompt 模板 + LLM 调用 | 模板变量完整性、意图识别单/多/超范围、规划、景点、汇总、总结模板（9 项） |

---

## 10. 技术栈

| 组件 | 技术 |
|---|---|
| LLM | 通义千问 `qwen3.7-max` + `text-embedding-v3`（DashScope API） |
| 代理框架 | LangChain 1.x `create_agent`（底层 LangGraph，`recursion_limit=15`） |
| 代理通信 | `python_a2a`（A2A 协议，AgentCard / Task / artifacts） |
| 工具服务 | FastMCP（MCP 协议，REST `/tools` 暴露 Schema） |
| 数据库 | MySQL 8.0（Docker，端口 3307） |
| 向量库 | Milvus v2.4.4（旅游团语义搜索，IVF_FLAT + COSINE） |
| 缓存/队列 | Redis（docker-compose 已部署，代码预留） |
| 天气数据 | 和风天气 API（30 天预报） |
| Web | FastAPI + Uvicorn + SSE |
| 前端 | 原生 HTML/CSS/JS 单页 |

---

## 11. Git 历史（分支 dev_gz7）

| 提交 | 说明 |
|---|---|
| `91f314b` | feat: LangChain 0.x 升级到 1.x + LangGraph create_agent |
| `78c8227` | fix: SSE 心跳保活 + send_task_async 阻塞修复 + 子进程 PIPE 排空 + 面试题 Q1-Q12 |
| `ab4629d` | feat: 完成 A2A+MCP 多代理旅行助手系统 |
| `7b4322f` | feat: 新增票务 MCP 服务器和行程 MCP 服务器 |
| `2cdeb01` | feat: 新增天气 MCP 服务器和 JSON 格式化工具 |
| `e2b0f94` | feat: 新增旅游团 RAG 初始化、数据导入脚本和天气爬虫 |
| `0efe605` | feat: 添加数据库建表脚本、Docker Compose 部署配置和日志模块 |
| `861f5d4` / `5ad78b0` | 初始提交 |

> 注：工作区当前有未提交改动（README.md、ticket_server.py、weather_server.py、config.py 已修改，.idea/ 未跟踪）。

---

## 12. 已知 TODO 与优化方向（源码注释整理）

**TODO 遗留**：
- `config.py`：确认模型名称；数据库端口 3306/3307 一致性；不用 root 账号
- `mcp_ticket_server.py` / `mcp_trip_server.py`：预订类工具均为模拟返回，未实际写库
- `chat_service.py`：`complex` 意图命中时当前不跳过规划（TODO 已标注）
- `ticket_server.py`：6 个 skill 可合并为 2 个

**README 中提出的优化方向**：
1. MCP 工具结果缓存（Redis），避免重复查库
2. 流式 Tool Calling（边查边输出）
3. A2A 代理健康检查与自动降级
4. 小模型做意图分类、大模型只生成回复（降延迟降成本）
5. 短期记忆滑动窗口 / 摘要压缩

**已解决的经典问题（README Q13）**：火车票查询超时 —— 3 个叠加 bug：
1. `send_task_async` 名为 async 实为同步阻塞 → `run_in_executor` 包装
2. 子进程 stdout/stderr PIPE 缓冲区满导致 print 阻塞 → stdout 丢弃 + stderr 后台线程排空
3. Docker 容器未就绪先启动服务 → 先 `docker compose up -d` 再 `python api_server.py`

---

*文档生成时间：2026-08-06（基于工作目录 `F:\智慧旅行agent_project\07-live_code\smart_voyage_gz7` 全量源码阅读）*
